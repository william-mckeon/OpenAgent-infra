[pytest]
# asyncio_mode=auto lets pytest-asyncio run `async def` tests marked with
# @pytest.mark.asyncio (used for the verify_api_key coroutine tests) without
# requiring an explicit event-loop fixture.
asyncio_mode = auto
testpaths = tests
