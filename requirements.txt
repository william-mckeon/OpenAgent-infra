# =============================================================================
# requirements.txt  (project root)
#
# openagent-infra is a pure-Python async proxy. No GPU/ML libraries —
# no torch, transformers, accelerate, or CUDA tooling — all inference runs
# at the BYOC provider. Pinned to compatible ranges.
# =============================================================================

fastapi>=0.110,<1.0          # web framework for the proxy API (bundles pydantic)
uvicorn[standard]>=0.29,<1.0 # ASGI server that runs the FastAPI app
httpx>=0.27,<1.0             # async HTTP client for streaming to the provider
python-dotenv>=1.0,<2.0      # loads .env for local (non-Docker) development